% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_list2xlsx.R
\name{write_sheet}
\alias{write_sheet}
\alias{write_sheets}
\alias{write_list2xlsx}
\title{write_sheet}
\usage{
write_sheet(d, file, sheetName, overwrite = FALSE)

write_sheets(lst, file, .progress = "none", show = FALSE, overwrite_wb = TRUE)

write_list2xlsx(
  lst,
  file,
  .progress = "none",
  show = FALSE,
  overwrite_wb = TRUE
)
}
\arguments{
\item{d}{A data frame.}

\item{file}{A file path string or an existing \code{Workbook} object.}

\item{sheetName}{Sheet name string.}

\item{overwrite}{if TRUE and \code{sheetName} already exists, replace it.}

\item{lst}{List of data frames. Names are used as sheet names.}

\item{.progress}{name of the progress bar to use, see create_progress_bar.}

\item{show}{open the file after saving (only applies when \code{file} is a path).}

\item{overwrite_wb}{if TRUE (default) always create a fresh workbook when
\code{file} is a path, even if the file already exists. Set to FALSE to
load and append to an existing file.}
}
\value{
The (modified) \code{Workbook} object, invisibly.

The (modified) \code{Workbook} object, invisibly.
}
\description{
Write a single data frame into one sheet of a Workbook.
\code{wb} can be a \code{Workbook} object or a file path string. When a file path is
given, the workbook is loaded (or created if the file does not exist),
the sheet is written, and the file is saved back automatically.

Write a list of data frames into a Workbook, one sheet per
element. \code{wb} can be a \code{Workbook} object or a file path string. When a file
path is given, the workbook is loaded (or created if absent), written, and
saved back automatically. The file extension is normalised to \code{.xlsx}.
}
